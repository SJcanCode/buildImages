
name: build
on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Install snapd
        run:  apt install snapd
      - name: Install docker 
        run: snap install docker
